export function IdleHero() {
  return (
    <div className="cosign-idle">
      <div className="cosign-idle-eye" aria-hidden />
      <h1>Keep authorship of the decisions.</h1>
      <p>
        Paste a Figma component link above. Cosign reads it, codes it, and shows you a ledger of every
        decision that went into the result — what came from Figma, what it interpreted, and what it
        decided on its own.
      </p>
      <small>
        In Figma → right-click your component → <strong>Copy link to selection</strong>.
      </small>
    </div>
  );
}
